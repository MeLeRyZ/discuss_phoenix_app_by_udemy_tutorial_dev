<%= form_for @changeset, topic_path(@conn, :create), fn f -> %>
    <div class="form-group">
        <%= text_input f, :title, placeholder: "Title", class: "form-control" %>
        <%= error_tag f, :title %>
    </div>
    
    <%= submit "Save Topic", class: "btn btn-primary" %>
<% end %>
<!---
1. form_for(@changeset, post_path(conn, :create), fn f -> ..otherhtml.. end )
2. form_for - create a form using this changest
3. get the url for func <- topic_path = "/topic/", :post
--->
